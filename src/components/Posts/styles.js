import { makeStyles } from "@material-ui/core";

export default makeStyles(()=>({
    mainContainer: {
        display:'flex',
        alignItems:'Center',
    },
    smMargin: {
        margin: theme.spacing(1),
    },
    actionDiv: {
        textAlign: 'center',
    },
}))